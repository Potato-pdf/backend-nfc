# Configuración del servidor
PORT=3000

# Conexión a la base de datos (PostgreSQL)
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://nfc_user:nfc_password@localhost:5432/nfc_db
