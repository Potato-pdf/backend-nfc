version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: nfc_user
      POSTGRES_PASSWORD: nfc_password
      POSTGRES_DB: nfc_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
